# Equinox dashboard (React)

The React frontend for the Equinox option pools on Arbitrum Sepolia — a static Vite 7 + React 19 + Tailwind 4 site on top of the **existing data layer in `../web/src`** (viem 2, block-pinned snapshots, event scan, trade builders, wallet wrapper). No backend, no telemetry, no third-party scripts: the only network calls are the public RPC and, later, the injected wallet. Contract: [`docs/FRONTEND_BRIEF.md`](../docs/FRONTEND_BRIEF.md); design: [`docs/superpowers/specs/2026-09-20-equinox-plan5-react-frontend-design.md`](../docs/superpowers/specs/2026-09-20-equinox-plan5-react-frontend-design.md).

State of this folder: every route is **live** on `ChainProvider` — the shell (sidebar, topbar, testnet strip, RPC banner, footer), Overview, Boards, Trade, Portfolio, Activity and Contracts (one block-pinned snapshot per 15-s poll, parity, gas, events; wallet actions through `useTrade`, one at a time, every one simulated first); loading, first-load error, stale and live states are explicit (`—` + reason, never a fabricated number), and nothing is shown for an account that is not connected.

Trade mirrors the classic panel (`web/src/panels/trade.ts`): pool switch with the asset per pool (mock USDG with the mint button on A/B, the `faucet.paxos.com` link on C), approve on the pool's own asset when the allowance is below 1,000,000, deposit/redeem with `previewDeposit`/`previewRedeem`, buy/close with the indicative view quote **and** the executed-path simulation ("executed ≈ … (max …)" / "(min …)") whose 1 % caps are what the calldata carries, claim for settled series, and a transaction log with decoded reverts and Arbiscan links. Previews are re-issued on every new snapshot; `#/trade?pool=B&series=3` prefills the form. Portfolio reads balances, LP shares × NAV/share, allowance, positions (with `quoteClose(id, units)` read on demand per position) and the account's own events.

Activity lists every Bought/Closed/Settled/Claimed event of every pool (newest first, filters by pool, kind and — with a wallet — "mine", explorer links for tx and actor; the footnote names the build-time seed's date and last block when one was loaded) and draws the σ_base history of the shared engine on a block axis with one marker per `Settled` event; a block's time is *approximated* by linear interpolation between two anchors — the deploy block and its `deployedAt` from the manifest, and the snapshot block and its block time — and always shown with "≈" (0.25 s per block is only the fallback when the anchors are degenerate). Contracts lists every address of the manifest (pool, option token and math per pool; engine, both math contracts, Chainlink feed, mock USDG, Paxos USDG when Pool C exists, mock sequencer feed, deployer) with Arbiscan and Sourcify links, the live `cfg()` of each pool and `params()` of the engine with units, the build commit/time, the documentation links and the classic honesty paragraph; the pool factory is not in the manifest and is therefore not listed.

The 1-s clock lives in `ClockProvider`/`useNow()` only: `ChainProvider` never ticks, so a `useChain()` consumer re-renders on data changes (snapshot, error, events, wallet) and nothing else; ages, countdowns and the live/stale badge combine `useNow()` with `meta.lastOkMs` (`isStale`). The EIP-1193 listener registered by the provider is released on unmount through the unsubscribe returned by `onWalletEvents` (`removeListener`), so React StrictMode's dev-only double mount registers it once — `loadSeed()` still runs twice in dev (harmless: the seed is merged with dedupe).

Routes keep any query **inside the hash** (`#/trade?pool=B&series=3`, `#/boards?board=1`): `client/src/lib/route.ts` builds those links (`tradeHref`, `boardsHref`), parses them (`hashQuery`, `useHashQuery`, validators against the manifest) and provides the router hook `useHashRoute` (path without `?…`). Such links are plain `<a href>`; wouter's `navigate()` would move the query into `location.search`, where the data layer reads `?rpc=`/`?poll=`.

## Commands

```sh
cd equinox-dashboard
(cd ../web && npm ci)   # the data layer resolves viem from web/node_modules
npm ci
npm run dev             # http://localhost:5174/equinox/  (hash routes: /#/, /#/boards, /#/trade, /#/portfolio, /#/activity, /#/contracts)
npm run check           # tsc --noEmit — type-checks client/src, test/ and ../web/src together
npm test                # vitest (jsdom, @testing-library/react)
npm run build           # vite build → dist/, then scripts/size.mjs (fails above 250 KB gzip of JS)
npm run preview         # serves dist/ at http://localhost:4174/equinox/
npm run seed            # ../web `npm run seed` → client/public/events-seed.json (git-ignored)
npm run size            # re-run the bundle budget check on an existing dist/
```

## Aliases (single source of chain access)

| Alias | Target | Why |
|---|---|---|
| `@` | `client/src` | app code |
| `@chain` | `../web/src` | `chain/*`, `deployment.ts`, `abi/*`, `ui/format.ts`, `ui/poll.ts` — imported, never copied |
| `@deployment` | `../deployments/arbitrum-sepolia.json` | the manifest: pools, assets, engine, boards, series ids |

`vite.config.ts` and `vitest.config.ts` define the aliases; `tsconfig.json` mirrors them in `paths` and includes `../web/src` (minus `main.ts` and `panels/`, the classic UI) so the data layer is type-checked with the app. `__COMMIT__` / `__BUILD_TIME__` are `define`d at build time (their ambient declarations live in `web/src/vite-env.d.ts`; `client/src/vite-env.d.ts` only references `vite/client`). `resolve.dedupe` keeps one copy of `viem`, `react` and `react-dom` at runtime.

Rules that follow: no chain logic in components (hooks over `@chain` only), no hard-coded addresses, counts or parameters (grep-checked at acceptance), copy per brief §7 (testnet always visible, mock USDG on A/B vs Paxos USDG on C, indicative vs executed, K5 = math parity not quote parity).

## Fallback rule

GitHub Pages keeps deploying the classic UI in `web/` until this app passes **every** item of brief §9 against the live chain (recorded in `ACCEPTANCE.md`); the switch is one commit in `.github/workflows/pages.yml`. If the app has not passed by 28 Sep 2026 the submission uses the classic UI. CI job `app` in `.github/workflows/ci.yml` runs `check`, `test` and `build` (with the bundle budget) on every push.

## Layout

```
client/index.html            fonts (Instrument Sans, Martian Mono), favicon, no analytics
client/src/main.tsx          StrictMode → App
client/src/App.tsx           ThemeProvider (dark) → TooltipProvider → Toaster → ChainProvider → ClockProvider → Router(useHashRoute) → Layout → routes
client/src/chain/            ChainProvider (no tick; seed merged + exposed as `seed` metadata; wallet listener released on unmount) + useChain, useSnapshot/useEvents/useWallet/useTrade (actions return a guard string when busy), selectors (pure + hooks; `useEngine(nowMs)`), clock (1-s ClockProvider/useNow, separate from the chain context)
client/src/components/       Layout (live shell, WalletButton, ≤ 680 px navigation drawer), Banner (RpcBanner), MetricCard, PoolCard, SigmaChart (JSX port of web/src/ui/svg.ts — no recharts; order or block axis, Settled markers, ≈ time labels), SigmaTimeline (Activity: SigmaChart in block mode), EventsTable (full event feed with ≈ time, actor, show more; event cards on phones), ParityPanel, EventsPreview, BoardHead (board panel header: one-line expiry, short pill on phones), BoardSummary, BoardTable (one board panel: full series table, expand, Trade link), SeriesCards (≤ 640 px card list fed by the same cell helpers as the tables), SeriesDetail (Greeks per pool + K5 prices), Scroller (wide-table scroll frame with a right-edge fade while columns are hidden), PoolSwitch (radiogroup per POOL_KEYS with the asset word), AmountInput (decimal input + right adornment: max + unit), PreviewLine (value / error / … / idle), TxLog (aria-live log, ✓/✗/…, explorer links), primitives (AppMark, SectionHeading, EmptyValue, SkeletonLine, StatusPill, MiniSparkline, keepSymbols), ErrorBoundary, ui/* (shadcn: button, badge, input, tooltip, sonner, select, tabs, dialog, skeleton, table)
client/src/hooks/            useMediaQuery (matchMedia as an external store; false without matchMedia → desktop in jsdom), usePhone (≤ 640 px), useSyncStatus (connecting|live|stale|failed from ChainState + useNow, shared by the sidebar card and the topbar badge)
client/src/lib/              sync (connecting|live|stale|failed + banner model), format (grouping, bps → %, exact WAD, build stamp, expiry label), boards (cell semantics of web/src/panels/board.ts, board pill long/short, filters, footnote), route (query-in-hash links, parsers, useHashRoute/useHashQuery), trade (classic preview/log sentences, select options, LP value, faucet links), activity (event filters, block → ≈ time, Settled markers, scan notes), contracts (manifest address rows, Sourcify/commit links, cfg()/params() rows with units, docs, honesty sentences)
client/src/pages/            Overview, Boards, Trade, Portfolio, Activity, Contracts — all live
client/src/index.css         Tailwind 4 + the "dark observatory" design system (custom classes); phone rules at ≤ 680 px (drawer, topbar) and ≤ 640 px (cards, type scale, 44-px targets)
test/                        vitest + jsdom (test/setup.ts loads jest-dom matchers); fixtures/ (snapshot + events from Appendix C), render.tsx (ChainContext + frozen clock), viewport.ts (matchMedia mock for phone layouts), chain/, components/, lib/, pages/
scripts/size.mjs, seed.sh    bundle budget, event seed copy
```

## History

- **20 Sep 2026 — scaffold.** Generated by Manus from `docs/FRONTEND_BRIEF.md` (React 19, Vite 7, Tailwind 4, shadcn/Radix, pnpm; 87 files). Its own visual verification notes: desktop overview at 1280 px renders the dark observatory direction consistently (fixed sidebar, topbar, testnet strip, hero, metric cards, chart panel, pool cards, board tables, parity panel, activity panel, footer); mobile overview at 375 px collapses the sidebar behind a hamburger, the connect button becomes icon-only, the top strip wraps, metric cards go two-column without horizontal page scroll; numeric on-chain values were intentionally `—`/empty because no RPC adapter existed, surfaced by a visible notice; `pnpm check` and `pnpm build` passed with a Vite size warning (660.10 kB raw / 174.98 kB gzip JS).
- **21 Sep 2026 — Plan 5 Task 1.** Manus runtime, Express server, OAuth constants, analytics, patches and 43 unused shadcn components removed; pnpm → npm; `@chain`/`@deployment` aliases; wouter hash router; `Home.tsx` split into `Layout`, `primitives` and `Overview` with every hard-coded value replaced by `—` + reason; vitest smoke test; CI job `app`. Bundle after the split: 341 KB raw / 108 KB gzip JS (budget 250 KB).
- **21 Sep 2026 — Plan 5 Task 2.** `ChainProvider` (port of `web/src/main.ts` + wallet part of `panels/trade.ts`), thin hooks, selectors, `useTrade`, fixtures; `web/src/chain/snapshot.ts` extended with `cfg()` and the full `params()`.
- **21 Sep 2026 — Plan 5 Task 3.** Layout and Overview bound to the provider: sync status (connecting/live/stale/RPC error) with a separate 1-s `ClockProvider`, RPC banner (first-load error with retry; "RPC unreachable — showing data fetched HH:MM:SS UTC" with the data kept), pool cards from `cfg()` bps, σ_base chart from `Observed` events (JSX, recharts dropped), parity n ✓ / n live + gas line, ATM ± 2 board summary, 5-event preview. 77 tests; bundle 680 KB raw / 205 KB gzip JS.
- **21 Sep 2026 — Plan 5 Task 4.** Boards page: one panel per manifest board with the full series table (Buy per pool or the human revert text, Δ A|B, Close, K5 parity, OI, σ_buy), row states (open / blackout / expired — awaiting settle / settled @ payout), ATM highlight, expandable Greeks per pool with the Solidity/Stylus prices (exact WAD on a mismatch), filter bar (board, calls/puts, open only), `#/boards?board=` deep link and `#/trade?pool=&series=` prefill links; `useHashRoute` keeps queries inside the hash. 98 tests; bundle 694 KB raw / 208 KB gzip JS.
- **21 Sep 2026 — Plan 5 Task 5.** Trade page (pool switch with the asset per pool, faucet button or Paxos link, approve on the pool's asset, deposit/redeem/buy/close/claim with the classic previews — indicative quote plus the executed-path "executed ≈ … (max/min …)" — inline guards, `canAct` gating, transaction log with decoded reverts, `?pool=&series=` prefill, previews re-issued per snapshot) and Portfolio page (balances per pool, LP shares × NAV/share, allowance, positions with `quoteClose(id, units)` on demand, claimable payouts, own history). Stub-provider drive (scratchpad): nothing signs, the close calldata's `minProceedsAssets` equals `minProceeds(executedClose)`. 119 tests; bundle 729 KB raw / 218 KB gzip JS.
- **21 Sep 2026 — Plan 5 Task 6.** Activity page (event table with pool / kind / "mine" filters, ≈ time per block from the snapshot, actor links, show-more paging, honest scan states; σ_base timeline = the same `SigmaChart` on a block axis with `Settled` markers and ≈ time labels) and Contracts page (every manifest address with Arbiscan + Sourcify links, live `cfg()` per pool and `params()` with units, build, docs, honesty paragraph). `ComingSoon` removed. 136 tests; bundle 752 KB raw / 224 KB gzip JS — no lazy loading needed.
- **21 Sep 2026 — Plan 5 Task 6b (mobile polish).** Phones (≤ 640 px): metric cards before a compact hero, series tables replaced by a card list (`SeriesCards`, same cell helpers as the tables, "Details" opens the same `SeriesDetail`), board header with a one-line expiry ("25 Sep 2026 · 08:00 UTC") and a short status pill, wrapping filter groups, stacked pool cards with two equal action buttons, Trade intro folded into a notice card with a "How it works" disclosure, full-width 44-px action buttons and 44-px inputs, event / address (with Copy) / cfg() cards on Activity and Contracts, 12-px body copy. ≤ 680 px: sticky topbar with a compact sync badge and an icon-only (< 400 px) or "Connect" wallet button; the navigation drawer is always the full variant (labels, network card, wallet row, callout, build + GitHub), closes on X / backdrop / Escape / route change, locks page scroll and returns focus to the hamburger. Everywhere: labels with Greek letters are never uppercased (`σ` stays `σ` via `.sym`), big numerals tightened (letter/word spacing), tables keep a right-edge fade while columns are hidden (`Scroller`). 148 tests; bundle 763 KB raw / 227 KB gzip JS.
- **21 Sep 2026 — Plan 5 Task 6c (review sweep).** Provider: the per-second `tick` is gone (clock only in `ClockProvider`; `engineView`/`useEngine` take `nowMs`), the build-time seed is merged with `mergeEvents` (never overwrites a scan that finished first, never lowers the scan cursor) and exposed as `seed { generatedAt, lastBlock }` through `useEvents()`, the wallet listener is released on unmount (`onWalletEvents` in `web/src/chain/wallet.ts` now returns an unsubscribe). `boardViews` indexes `pools[k].boards` by manifest position; `useTrade` actions return a guard string while an action is in flight. Pages: the Boards skeleton follows the `?board=` / type filter; Portfolio's `quoteClose` reads are pinned to the snapshot block and its Trade link uses `DEFAULT_TRADE_POOL`; the transaction log's `aria-live` list is always rendered; Trade never picks a series silently (`pickOption` → null + a "pick …" placeholder, guard on click); amount inputs carry the unit in their accessible name; the Activity "Show more" page survives event polls (reset only on a filter change); block → ≈ time is a two-anchor interpolation (deploy block/`deployedAt` ↔ snapshot block/time) and the Activity footnote names the seed date; `DocLink.icon`; `useSyncStatus()` shared by the sidebar card and the topbar badge; `WalletButton` on `useWallet()`; no `meter` role before the first snapshot. 152 tests; bundle 764 KB raw / 228 KB gzip JS.

<!-- trace: idea="Equinox — an options AMM for ETH on Arbitrum whose prices are computed entirely on-chain (Black-Scholes in Stylus, volatility from Chainlink prints, no IV oracle)" | event="Arbitrum Open House Singapore: Online Buildathon" | deadline="2026-10-01 23:59 SGT (15:59 UTC) per T&C; HackQuest shows 2026-10-04 23:59 SGT — unresolved" | source=submission-packager -->
# Rubric scorecard — Arbitrum Open House Singapore: Online Buildathon

**Project scored:** Equinox as built on 2026-09-20 (repo `nodesproof/equinox`, branch `feat/plan-3b` @ `f51c20e`, 81 commits; two pools + cached Stylus program live on Arbitrum Sepolia since 2026-09-19/20; dashboard built but **not yet published** — GitHub Pages returned 404 on 2026-09-20 07:45 UTC because PRs #4, #5 and the Plan 3b PR are unmerged; **no video yet**; first real settlement scheduled Fri 25 Sep 2026 08:00 UTC).
**Deadline planned against:** 2026-10-01 23:59 SGT (15:59 UTC, 22:59 WIB) per the Singapore T&C. HackQuest shows 2026-10-04 23:59 SGT. Not resolved here — confirm with the organizers (Discord `#open-house`, engineering@arbitrum.foundation) and keep the conservative date until they answer in writing.

## Judging weights

**JUDGING WEIGHTS NOT PUBLISHED.** Two official rubrics exist, neither carries weights (`../../docs/HACKATHON_BRIEF.md` › Judging criteria). Both are quoted verbatim below and Equinox is scored against both. The percentages on Rubric A are the brief's **ESTIMATED** working split (30 / 30 / 15 / 25) — a model, not ground truth; do not repeat them to a judge.

Rubric A — HackQuest page (quoted): "Projects are assessed on the following criteria: **Smart contract quality -** code following best practices, structured logically and efficiently, with minimal security vulnerabilities · **Product-Market Fit -** projects with clear potential to attract and retain users · **Innovation and Creativity -** original approaches that push boundaries · **Real Problem Solving -** applications that address genuine market needs. Extra consideration is given to projects integrating Paxos' USDG stablecoin."

Rubric B — Singapore T&C §6.1.2 (quoted): "Projects will be evaluated based on the following criteria: ● Innovation ● Technical implementation ● Use of Arbitrum technology ● Potential impact ● Presentation quality ● Novelty projects will receive a bonus consideration"

Scores are 1–5 as three judge personas would give them after reading the HackQuest entry, opening the dashboard, and (technical judge only) running `forge test` / `tools/bench/onchain-check.sh` / the README `cast call`: an Arbitrum Foundation / Offchain Labs engineer (the Stylus-literate judge — "does it need to be on-chain, and does it need Stylus?"), a founder-program judge ("who wants this, and what is the roadmap?"), and a sponsor DevRel judge (OpenZeppelin security hygiene; Paxos "is that real USDG?"). Archetypes from the brief — no roster is published.

## Rubric A — HackQuest criteria (weights ESTIMATED)

| Criterion (verbatim) | Weight | Evidence the judge will SEE (file / URL / number) | Self-score 1–5 — one-line justification | Cheapest lift |
|---|---|---|---|---|
| **Smart contract quality** — "code following best practices, structured logically and efficiently, with minimal security vulnerabilities" | ESTIMATED 30 % | `forge test`: **87 passed, 0 failed, 9 suites** (re-run 2026-09-20); 7 invariants (INV-1 solvency, 2 reserve bookkeeping, 4 supply = OI, 10 σ bounds, 11 escrow, 15/16 NAV-non-decreasing and buy ≥ close) at 32 × 128 in CI and 256 × 200 = 51,200 calls each in the long run, 0 reverts (`contracts/test/EquinoxPool.invariants.t.sol`); 93.4 % line coverage on `src/pool` + `src/oracle` (PRD §12: Pool 92.1 %, VolEngine 98.6 %, OptionToken 100 %, OracleLib 100 %); `nonReentrant` on `buy`/`close`/`settle`/`claim` and all four ERC-4626 entries, CEI, `Ownable2Step`, hard bounds on every config/vol parameter and rate limits on `setParams` (`EquinoxPool.sol`, `EquinoxVolEngine.sol`); three documented review rounds with fixes (`c33e706` NAV marked at σ_mark(0), `f7661f7` lagged capital reference, `11511b9` prices never cross the mark, `c2fecdd`…`994b862` final wave) + an out-of-gas incident with a per-block post-mortem and a tooling fix (`docs/DEMO_LOG.md`, `tools/sepolia/lib.sh`); OpenZeppelin 5.4.0 vendored; CI enforces `forge build --sizes` (EquinoxPool 17,812 B; PoolDeployer margin 1,552 B), constants/vector drift, `cargo stylus check`; the math has one executable spec (`wad_emul.py`) and two bit-identical ports (23 Rust tests, 12 Foundry tests, 1,284 vectors) | **4** — the invariant/fuzz/review story is unusually strong for a solo hackathon build; the judge also finds **Indonesian NatSpec and comments** in every contract (`EquinoxPool.sol` line 33 "batas loop MtM", `bs-stylus/src/lib.rs` line 1), **no source verification on Arbiscan or Sourcify** (checked 2026-09-20: Sourcify `match: null` on the pools and the control), an EOA owner with no timelock (PRD §11 T14, honestly documented), an open `MockSequencerFeed.set` on the testnet, and no external audit | (a) **First**, verify the 12 Solidity contracts on Arbiscan from the current tree (`forge verify-contract --chain arbitrum-sepolia …`; the last change under `contracts/src` is `d3cf3cf`, immediately before the pool deploy `a78c066`, so HEAD compiles to the deployed bytecode; the pools/tokens/engine were created inside `PoolE2EDeployer`'s constructor, so pass `--constructor-args` from `cast abi-encode` of the on-chain values) and run `cargo stylus verify` for the program (~1–2 h). (b) **Only then** translate NatSpec + comments in `contracts/src/**` and `stylus/**` to English (~2–3 h; 87 tests stay green) — a comment edit is not bytecode-neutral: solc embeds a CBOR metadata hash of the sources in the runtime code, so translating first would break exact verification of the live deployment |
| **Product-Market Fit** — "projects with clear potential to attract and retain users" | ESTIMATED 30 % | Named users: USDG LPs who sell vol without trusting anyone's IV oracle, short-tenor ETH hedgers/traders, and integrators who call `quote`/`impliedVol` on the public Stylus library (PRD §4 personas, §14 "the library is the acquisition channel"); a working, trade-able product on Sepolia with a faucet and MetaMask flow (`web/src/panels/trade.ts`); honest economics (PRD §14: fee 300 bps of premium, "1 M USDG premium/month × 3 % = 30k — small; scale comes from underlyings and integrators"); competitors on Arbitrum with off-chain IV exist (Premia v3 SSVI surface oracle, Stryke CLAMM — verified 2026-09-20); **no users, no LP, no integrator, no external quote**; no delta hedger (LPs carry direction, like every DOV) | **3** — the buyer and the wedge ("audit the price from state") are clear and the product is usable today, but nothing on the page proves anyone will supply liquidity; the founder-program judge asks "who has asked for this?" and the answer is nobody in writing | Bring one external signal before the deadline: demo the dashboard at feedback session #2 or #4 (Sep 23 / Sep 28, 09:00 UTC), ask the Offchain Labs / Arbitrum team whether a tested Stylus quant library belongs in their resources list, and quote the answer in the description; add a 10-line "call the library from your contract" snippet (`IBlackScholes`) to the README (2–3 h total) |
| **Innovation and Creativity** — "original approaches that push boundaries" | ESTIMATED 15 % | σ derived on-chain from Chainlink prints (EWMA with irregular Δt, `EquinoxVolEngine.poke`, `ewmaUpdate` in Stylus) — the only external input is spot; the full quant loop on-chain per transaction (Greeks per quote, safeguarded-Newton IV solver 3–20 iterations, 32-series `markPortfolio` in one call, EWMA per observation); hard solvency without a hedger via capped calls priced as `C(K) − C(2K)`; three bit-identical implementations of one algorithm with a Python exact-integer spec; a measured, honest Stylus-vs-Solidity benchmark that retracted its own 10× target (PRD §2.4, §13 "Aturan kejujuran") and found `I256` arithmetic 3× dearer than EVM opcodes (105 vs ≈ 35 gas per mul+div pair); prior art credited, not hidden (PRD §1: Lyra v1 on-chain BS + inventory-impact IV, Lyra v2/Derive, Premia v3, Panoptic, Hegic); freshness: **0 options venues among the 18 submitted Singapore entries** (gallery 2026-09-20); Stylus-math neighbours only in London (OmniCurve, Ward, OmniVerse) | **4** — a genuinely new composition (endogenous σ → on-chain solver/MtM → hard solvency → measured benchmark); not 5 because each component has a named precedent (Lyra's inventory impact, Chainlink spot, ERC-4626/1155) and the "novelty bonus" depends on a judge who knows options venues | Nothing cheap moves this; keep the prior-art table visible and say "to our knowledge no bit-identical Stylus-vs-Solidity quant benchmark has been published" rather than "first ever" |
| **Real Problem Solving** — "applications that address genuine market needs" | ESTIMATED 25 % | The number a judge can recompute: on a 7-day 5 % OTM ETH call, ±10 vol points = +33.3 % / −31.4 % premium (PRD §6.7: 40.22 / 58.62 / 78.14 USDG), 16.7 % at the money — an IV oracle late by an hour is PnL, not a detail; documented industry evidence: Lyra v2/Derive left on-chain AMM pricing for an off-chain orderbook, Premia v3 runs a volatility-surface oracle (docs.premia.blue › Oracles, SSVI), Stryke CLAMM feeds IV "sourced from market data" into oracles (docs.stryke.xyz) — both verified 2026-09-20; the EVM cost argument is measured, not asserted (`impliedVol` 20 iterations 616,839 gas in Solidity vs 223,373 cached Stylus; `markPortfolio` 32 series 1,242,892 vs 427,448 — `docs/BENCHMARK.md`); the sandwich/dilution levers found in review are exactly the LP-sniping problem an on-chain NAV must solve (PRD §11 T6/T16) | **4** — the problem is documented with reproducible numbers and named venues; not 5 because the "genuine market need" evidence is documentary (competitor docs, a migration) rather than a loss event or a user asking, and PRD §18 V13 (Rysk, Moby IV sourcing) remains unverified | Put the 33 % sensitivity number and "Lyra moved off-chain; Premia and Stryke run IV oracles" in the first three lines of the HackQuest description (done in `SUBMISSION.md`); finish V13 for Rysk and Moby (30 min of docs reading) so the "every venue we know of" sentence has four sources |
| *Modifier — "Extra consideration is given to projects integrating Paxos' USDG stablecoin"* | tie-breaker, not a score | Settlement asset is USDG by design (6-decimal accounting `assetScale = 1e12`, PRD §14 names MAS/MiCA regulation as the reason over USDC, T9 documents Paxos pause/blocklist risk); on Sepolia it is `MockUSDG` (open `mint`, no pause/blocklist) because Paxos has no USDG there; no fork test against the real token; V9 (USDG on Arbitrum One) unverified | do **not** claim it by default — `SUBMISSION.md` › Part 3 (user decides; tick only with the caveat verbatim in field 4) | verify V9 (`cast call ⟨usdg⟩ "decimals()(uint8)" --rpc-url https://arb1.arbitrum.io/rpc`, 10 min) before any Q&A about production |

**Weighted projected total (ESTIMATED weights):** (0.30 × 4 + 0.30 × 3 + 0.15 × 4 + 0.25 × 4) / 5 = **74 %** — **AT RISK** against a 75 % bar (the bar = "top 3 of a ~45-project field where a non-placing London entry (CorpAction Engine) shipped 14 contracts and 94 % coverage", brief › Past winners pattern 7). The gap is PMF plus the presentation debt below; the technical axes are already at 4.
**Lowest criterion:** Product-Market Fit — no external adoption signal. Single cheapest move: one quoted reaction from the Arbitrum / Offchain Labs team at the Sep 23 or Sep 28 feedback session plus the "call the library" README snippet, written into the description before Oct 1.

## Rubric B — Singapore T&C §6.1.2 (unweighted)

| Criterion (verbatim) | Evidence the judge will SEE | Self-score 1–5 — justification | Cheapest lift |
|---|---|---|---|
| **Innovation** | as Rubric A row 3 | **4** — same reasoning | same |
| **Technical implementation** | Live Sepolia deployment: two pools (`0x627b…be08` control, `0x7f79…7cc9` Stylus), one shared `EquinoxVolEngine` `0xc331…6a3e` on the **real Chainlink ETH/USD feed** `0xd30e…5165` (2574.42 USD, round 120 s old at packaging), cached Stylus program `0xb3b3…51d4` (`codehashIsCached == true`, `programTimeLeft` 364 d), 12 series per pool, 1,000,000 USDG seed each; 20/20 on-chain exactness checks vs the Python spec on **both** math addresses (re-run 2026-09-20); Sepolia cached gas measured directly and equal cell-for-cell to the devnode-derived column (`docs/BENCHMARK.md` › "Verifikasi di Arbitrum Sepolia"); demo log with 7 Arbiscan-linked transactions (incl. the failed one) and the byte-identical `quoteBuy` 268.181826 USDG @ σ 0.667412 on both pools at block 310700824; keeper (`tools/keeper/keeper.sh`, cron */15 in `.github/workflows/keeper.yml`, gas pad 1.5×, self-healing sequencer mock, named revert selectors); dashboard reads everything via Multicall3, checks K5 parity per series live (`web/src/chain/parity.ts`; network test 3/3 green 2026-09-20), estimates `buy` gas A vs B (361,406 vs 318,602 at block 310802077), and drives MetaMask writes with simulate-before-sign and decoded reverts; CI green on every run (the last 15 runs on 2026-09-19/20 all `success`) | **4** — everything claimed is executable and linked; what a judge trips on today: the dashboard URL is 404 until the merges, the settlement/claim path has never run on a public chain (Friday), the E2E identity is "math parity" not "identical pools" (explained, but it needs the explanation), and the documents the tables live in (BENCHMARK, DEMO_LOG, OPS, PRD) are Indonesian | Merge the three PRs (Pages goes live automatically; 30 min); add an English "Summary" block at the top of `docs/BENCHMARK.md` and `docs/DEMO_LOG.md` (the numbers are already there; 1 h); run Friday's settlement + `--claim` and commit the log (`DEMO_RUNBOOK.md` › Timeline) |
| **Use of Arbitrum technology** | The thesis *is* Arbitrum technology: a `no_std` Rust Stylus program (`stylus/bs-stylus`, stylus-sdk 0.10.9, 34.4 KB → 2 fragments, needs Stylus v3 / ArbOS 61), deployed, activated (data fee 0.000152 ETH) and **cached** via `cargo stylus cache bid … 0` (`programInitGas` 30,919 → 4,623 measured through `ArbWasm`); stack pinned to 16 KiB so `programMemoryFootprint` = 1 page (17 pages = 17,000 gas saved per call, PRD §9.4); pool uses EIP-1153 transient storage for a single NAV evaluation per ERC-4626 call (ArbOS ≥ 20, cancun); `block.timestamp` only (Nitro `block.number` is an L1 estimate); a Nitro-specific finding written up — `eth_estimateGas` is the zero-margin minimum and rises with `block.timestamp`, hence the 1.5× pad (`DEMO_LOG.md` post-mortem); Chainlink Arbitrum Sepolia feed with the documented absence of an L2 sequencer uptime feed there; a Solidity control that proves Stylus was a measured choice, not decoration (PRD §16) | **4** — as deep a use of the Stylus/Nitro stack as this field is likely to see, measured rather than asserted; not 5 because nothing runs on Arbitrum One (the prize's own mainnet condition) and the library is not yet published for other Arbitrum contracts to call | Deploy and cache `bs-stylus` alone on **Arbitrum One** as the public library (`tools/devnode/deploy.sh arbitrum-one …` + `cargo stylus cache bid`; ≈ 1 h + activation fee ≈ 0.00015 ETH + deploy gas) and list the address in the README — it is the "distribution channel" of PRD §14 and a concrete step toward the T&C mainnet milestone; keep the pools on Sepolia |
| **Potential impact** | Category-opening claim: an options venue whose entire price path is auditable from state (PRD §14 "Jujur soal ekonomi"); the Stylus library as a public dependency for margin engines and structured vaults (PRD §4 U4, §14 Distribution); the benchmark as a public good for anyone moving DeFi math to Stylus (PRD §0 claim 3, §16); honest scale: 30k USDG/month at 1 M premium/month; expansion path BTC, perps-hedged tenors, tokenized equities with a session regime (PRD §14, README › Roadmap) | **3** — real if adopted, small per pool by the PRD's own admission; impact depends on LPs and integrators the project has not yet reached | Same as PMF: an external signal. Also cite the Arbitrum DeFi/RWA context from the organizer blogs in the description (brief › Judges: "Does it need to be onchain? … what unique problem does it solve?") |
| **Presentation quality** | English README with status table, live-addresses table, try-it `cast call`, architecture diagram, benchmark table and roadmap (`README.md`); dashboard with six panels (header · shared vol engine & NAV · 12-row series board with parity ✓ and gas line · Trade · Activity with σ_base chart · Contracts footer with the mocks disclosure) — **not yet published**; 300-char form answers prepared (`SUBMISSION.md`); **no video**; every supporting doc a judge clicks from the README (`docs/BENCHMARK.md`, `docs/DEMO_LOG.md`, `docs/OPS_SEPOLIA.md`, `docs/VERIFICATION.md`, `prd-arsitektur.md`) is in Indonesian; contracts unverified on the explorer; NatSpec Indonesian | **2 today / 4 after the fixes** — the README alone is strong, but the three surfaces a judge opens next (dashboard, explorer source, video) are respectively 404, bytecode-only and absent, and the fourth (docs) switches language | In order: merge → Pages live (30 min); record and cut the 3:00 video after Friday's settlement (`VIDEO_SCRIPT.md`, ~4 h); explorer verification (1–2 h); English summary blocks in BENCHMARK/DEMO_LOG and English NatSpec (3–4 h) |
| **Novelty bonus** — "Novelty projects will receive a bonus consideration" | as Innovation | likely — no options/derivatives venue among the 18 submitted Singapore entries; the London Stylus-math projects (OmniCurve: Gaussian-priced prediction market; Ward: vol-aware credit engine; OmniVerse: Rust AMM kernel) attack different problems | keep the differentiation lines from `JUDGE_QA.md` › Competitors in the description |

Structural rules (not scores): Equinox qualifies for the "At minimum, 1 of 3 prizes are reserved for a project building on Arbitrum" reservation, **not** the Robinhood Chain one, so it competes for at most 2 of 3 slots per track (brief › Podium arithmetic); the Promising Products Track is "evaluated separately" and exempt from the 25/25/50 milestone split; the Overall Prize releases 50 % only on "A successful mainnet launch on an Arbitrum Chain" — Equinox's mainnet path (Arbitrum One, cached program, real USDG) is specified in the README roadmap but not executed, and V9 (USDG on One) is unverified.

## What will lose us points

Ranked by points-per-hour (Δ on the ESTIMATED weighted total, or the T&C modifier, per hour of work):

| Rank | Weakest criterion | Cheapest lift | Effort | Expected gain |
|---|---|---|---|---|
| 1 | **Presentation quality** (T&C, scored 2 today) — the dashboard link is 404 until the PRs merge, there is no video, and the settlement/claim beat does not exist until Fri 25 Sep 08:00 UTC | Merge PRs #4 → #5 → Plan 3b by Wed 23 Sep (Pages + keeper cron go live from `main`); run Friday's settlement, `--claim`, list the 9 Oct and 16 Oct boards; record the five-section video per `VIDEO_SCRIPT.md` and upload to `demoVideo` | 30 min + Friday's 1 h + ~4 h video | +2 on Presentation quality (2 → 4); removes the only DQ-shaped risk in the package ("functional and demonstrable" needs the link to resolve) |
| 2 | **Product-Market Fit** (ESTIMATED 30 %, scored 3) — no adoption signal on the page | Attend feedback session #2 (Sep 23) or #4 (Sep 28, 09:00 UTC) with the live dashboard, ask the Offchain Labs / Arbitrum team the library question, quote the answer; add the `IBlackScholes` integration snippet to the README | 2–3 h | +1 on PMF (≈ +6 % weighted — the largest single move available) and +1 on Potential impact |
| 3 | **Smart contract quality / Presentation quality** — every explorer link resolves to unverified bytecode (Sourcify `match: null`, 2026-09-20), and the code a judge opens carries Indonesian NatSpec (`EquinoxPool.sol` header, `bs-stylus/src/lib.rs` line 1) | Verify the 12 Solidity contracts on Arbiscan + `cargo stylus verify` **before touching any source** (HEAD == deployed sources: the last `contracts/src` change `d3cf3cf` predates the deploy `a78c066`); then translate NatSpec/comments to English (87 tests green) — a comment edit changes solc's CBOR metadata hash in the runtime bytecode, so the reverse order breaks exact verification; add English summary blocks at the top of `docs/BENCHMARK.md` and `docs/DEMO_LOG.md` | 1–2 h + 3–4 h | +0.5 on Smart contract quality (≈ +3 % weighted) and +1 on Presentation quality; both are what the Arbitrum engineering judge reads first |

Also cheap and free: keep an open board on both pools through the judging window (board 1 expires Fri 2 Oct 08:00 UTC — inside the T&C judging window; list `1791532800:…` (9 Oct) and `1792137600:…` (16 Oct) after Friday's settlement, `DEMO_RUNBOOK.md` › Timeline), and top up the keeper wallet ≈ 5–6 Oct (runway ≈ 18 days from 20 Sep, `docs/OPS_SEPOLIA.md`) — a judge who lands on "no open series" or an unsettled expired board will not read further.
